[project]
name = "openfilter-mcp"
version = "0.1.0"
dependencies = [
    "code-context>=0.1.0",
    "fastmcp>=2.12.4",
    "gitpython>=3.1.45",
    "httpx>=0.28.1",
    "jq>=1.10.0",
]

[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project.scripts]
index = "openfilter_mcp.index:preindex_openfilter_repos"
serve = "openfilter_mcp.server:main"

[tool.uv]
config-settings-package = { "llama-cpp-python" = { "cmake.args" = "-DGGML_CUDA=on" } }

[tool.uv.sources]
code-context = { git = "ssh://git@github.com/plainsightai/code-context", rev = "modularity" }
